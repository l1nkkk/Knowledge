> 难度：中等


> 题目
```
给定两个以字符串形式表示的非负整数 num1 和 num2，返回 num1 和 num2 的乘积，它们的乘积也表示为字符串形式。

示例 1:
输入: num1 = "2", num2 = "3"
输出: "6"

示例 2:
输入: num1 = "123", num2 = "456"
输出: "56088"

```

> 代码
```cpp
class Solution {
public:
    string multiply(string num1, string num2) {
        if(num1 =="0" || num2 == "0") return "0";
        vector<int> res(num1.size()+num2.size(), 0);
        for(int i = num1.size()-1; i >= 0; --i){
            for(int j = num2.size()-1; j >= 0; --j){
                int mul = (num1[i]-'0') * (num2[j]-'0');
                int p1 = i + j;
                int p2 = p1 + 1;
                int sum = res[p2] + mul;
                res[p2] = sum%10;
                res[p1] += sum/10;
                // 注：这里不用计算p1的进位，后面的操作中会自动被处理。这个算法的特点
            }
        }
        int pos = 0;
        // 找到第一个不为0的
        while(res[pos] == 0){
            ++pos;
        }

        // vector<int> 转字符串
        string rtn;
        while(pos < res.size()){
            rtn += res[pos++]+'0';
        }
        return rtn;
    }
};
```

```
执行用时：4 ms, 在所有 C++ 提交中击败了94.61%的用户
内存消耗：6.6 MB, 在所有 C++ 提交中击败了66.57%的用户
```
