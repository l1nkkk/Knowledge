> 难度：简单
- dfs

> 题目、
```
给定两个整数 n 和 k，返回范围 [1, n] 中所有可能的 k 个数的组合。

你可以按 任何顺序 返回答案。

示例 1：
输入：n = 4, k = 2
输出：
[
  [2,4],
  [3,4],
  [2,3],
  [1,2],
  [1,3],
  [1,4],
]

```

> 代码

```cpp
class Solution {
public:
    vector<vector<int>> res;
    set<int> record;
    vector<vector<int>> combine(int n, int k) {
        dfs(n,k,0, 1);
        return res;
    }

    void dfs(int n, int k, int nl, int start){
        if(nl == k){
            res.push_back(vector<int>(record.begin(), record.end()));
            return;
        }
        for(int i = start; i <= n; ++i){
            if(record.find(i) != record.end())
                continue;
            record.insert(i);
            dfs(n, k, nl+1, i);
            record.erase(i);
        }
    }
};
```

```
执行用时：460 ms, 在所有 C++ 提交中击败了5.60%的用户
内存消耗：92.5 MB, 在所有 C++ 提交中击败了6.77%的用户
```